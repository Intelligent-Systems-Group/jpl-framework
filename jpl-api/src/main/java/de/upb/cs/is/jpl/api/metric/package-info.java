/**
 * Provides abstract classes, interfaces and {@link java.lang.Enum}(s) required for the
 * implementation of any type of evaluation metrics according to which the algorithm needs to be
 * evaluated. It also contains the evaluation metric configuration abstract classes which extends
 * abstract Json configuration class, which is used to convert the default json configuration for
 * the corresponding class and also to store some extra parameters required for running the storing
 * the results of the evaluation metric ran on an algorithm for a dataset.
 * 
 * @author Pritha Gupta
 *
 */
package de.upb.cs.is.jpl.api.metric;